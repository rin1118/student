<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="loginMapper">

	<!-- 학과명 SELECT  -->
	<select id="getDepartment" resultType="com.test.vo.DepartmentVO">
		select * from MAJOR order by name
	</select>
	
	<!-- 회원가입 -->
	<insert id="join">
		insert into student (s_no, name, email, d_name, id, password) values (student_seq.nextval, #{name}, #{email}, #{d_name}, #{id}, #{password})
	</insert>
	
	<select id="login" resultType="com.test.vo.LoginVO">
		select s_no, name, email, d_name, id, password from student where id = #{id} and password = #{password}
	</select>
</mapper>